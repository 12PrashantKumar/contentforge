moved govprep's vector store from ChromaDB to Postgres + pgvector.

ingestion → local mpnet embeddings → pgvector → hybrid search (BM25 + dense + RRF). retrieval score didn't budge. that wasn't the point — the point was killing a service. one database now instead of two.